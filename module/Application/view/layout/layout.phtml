<?php
$this->headTitle('DEAL.BZH - Échange et partage de matériel');
$this->headMeta()->appendName('viewport', 'width=device-width, initial-scale=1.0');
$this->headMeta()->appendHttpEquiv('Content-Type', 'text/html; charset=UTF-8');
?>
<!DOCTYPE html>
<html lang="fr">
<head>
    <?= $this->headMeta() ?>
    <?= $this->headTitle() ?>
    <?= $this->headLink()->prependStylesheet($this->basePath('css/deal-bzh.css')) ?>
    <?= $this->headScript() ?>
</head>
<body>
    <header class="main-header">
        <div class="container">
            <div class="logo">
                <a href="<?= $this->url('home') ?>">
                    <img src="<?= $this->basePath('deal_bzh_logo-ok.svg') ?>" alt="DEAL.BZH" />
                </a>
            </div>
            <nav class="main-nav">
                <ul>
                    <li><a href="<?= $this->url('home') ?>">Accueil</a></li>
                    <li><a href="<?= $this->url('deal') ?>">Annonces</a></li>
                    <li><a href="<?= $this->url('forum') ?>">Forum</a></li>
                    <?php 
                    try {
                        $identity = $this->identity();
                        if ($identity): ?>
                        <li><a href="<?= $this->url('deal/create') ?>">Déposer une annonce</a></li>
                        <li><a href="<?= $this->url('message') ?>">Mes messages</a></li>
                        <?php if (method_exists($identity, 'isAdmin') && $identity->isAdmin()): ?>
                            <li><a href="<?= $this->url('admin') ?>">Administration</a></li>
                        <?php endif; ?>
                    <?php 
                        endif;
                    } catch (\Exception $e) {
                        // AuthenticationService non disponible, continuer sans menu utilisateur
                    }
                    ?>
                </ul>
            </nav>
            <div class="user-menu">
                <?php 
                try {
                    $identity = $this->identity();
                    if ($identity): ?>
                        <span class="user-name"><?= $this->escapeHtml(method_exists($identity, 'getEmail') ? $identity->getEmail() : 'Utilisateur') ?></span>
                        <a href="<?= $this->url('user/logout') ?>">Déconnexion</a>
                    <?php else: ?>
                        <a href="<?= $this->url('user/login') ?>">Connexion</a>
                        <a href="<?= $this->url('user/register') ?>">Inscription</a>
                    <?php endif;
                } catch (\Exception $e) {
                    // AuthenticationService non disponible, afficher les liens de connexion
                    ?>
                    <a href="<?= $this->url('user/login') ?>">Connexion</a>
                    <a href="<?= $this->url('user/register') ?>">Inscription</a>
                <?php } ?>
            </div>
        </div>
    </header>

    <main class="main-content">
        <div class="container">
            <?= $this->content ?>
        </div>
    </main>

    <footer class="main-footer">
        <div class="container">
            <p>&copy; <?= date('Y') ?> DEAL.BZH - Plateforme d'échange et de partage de matériel</p>
        </div>
    </footer>

    <?= $this->inlineScript() ?>
</body>
</html>

